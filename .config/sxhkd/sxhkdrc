super + Escape
        pkill =USR1 -x sxhkd

# Launching Programs
# Browser
super + b
      qutebrowser
# Ranger
super + period; period
       kitty -e ranger
super + period; i
       kitty -e ranger ~/Dropbox/0_Inbox
super + period; n
       kitty -e ranger ~/Dropbox/vault/0-Fleeting-Notes
super + period; d
       kitty -e ranger ~/Downloads
super + period; l
       kitty -e ranger ~/Documents/Library/pdf
# File Browser
super + n
      nautilus
# Rofi emoji
super + m
      rofi -modi emoji -show emoji
# # Freetube
 super + y
       freetube
# # Gpick
 super + g
       gpick
# Rofi
# Run Launcher
super + x
       rofi -show run
# Window swticher
super + comma
       rofi -show window
# Clipboard
super + c
       rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
# Searchweb
super + slash
      qbrofi
# Pass
super + p
        dmenu_pass
# Screenshots
Print
    xfce4-screenshooter
#
super + shift; p
      xfce4-screenshooter
# Screenlock
super +z
      xflock4
# Emacs (super + e followed by another key)
super + e; e
  emacsclient -c -a 'emacsclient --eval "(*scratch*)"'
super + e; b
    emacsclient -c -a 'emacs' --eval '(ibuffer)'
super + e; d
    emacsclient -c -a 'emacs' --eval '(dired nil)'
super + e; l
   emacsclient -c -a 'emacs' --eval '(elfeed)'
super + e; n
    emacsclient -c -a 'emacs' --eval '(denote)'
super + e; m
    emacsclient -c -a 'emacs' --eval '(my-mu4e-inbox)'
super + e; c
    emacsclient -c -a 'emacs' --eval '(compose-mail)'
super + e; j
    emacsclient -c -a 'emacs' --eval '(org-journal-open-current-journal-file)'
#super + e; t
#    emacsclient -c -a 'emacs' ~/Dropbox/vault/todo.org
# i3 windows
super +w; x
 i3-msg kill
