# sxhkd reload configuration files:
super + Escape
	pkill -USR1 -x sxhkd
# Terminal
super + Return
    wezterm
# Browser
super + b
    qutebrowser
# Ranger
super + period
    wezterm -e ranger
# Rofi emoji
super + m
    rofi -modi emoji -show emoji
# Run Launcher
super + x
    rofi -show run
# Window switcher
alt + grave
    rofi -show window
# Searchweb
super + slash
    searchweb
# rofi bookmarks
super + shift + slash
    bookmarks-rofi
# Pass
super + p
    dmenu_pass
# Screenshot
Print
    xfce4-screenshooter
# Screenlock
super +z
    xflock4
# bash clock
super + c
    bash -c "notify-send -t 5000 \"$(date +'%Y-%m-%d  %H:%M')\" -u normal"
super + space
    scratchpad
# GUI File mananger
super + n
    thunar
# Settings
super + s 
    xfce4-settings-manager
# Zet
super + shift + n
    wezterm -e zet
# Log out
ctrl + alt + Delete
    xfce4-session-logout
