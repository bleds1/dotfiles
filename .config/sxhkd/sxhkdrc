super + Escape
        pkill =USR1 -x sxhkd

# Launching Programs
# Browser
super + b
      qutebrowser
# Ranger
super + period; period
       kitty -e ranger
super + period; i
       kitty -e ranger ~/Dropbox/0_Inbox
super + period; d
       kitty -e ranger ~/Downloads
super + period; l
       kitty -e ranger ~/Documents/Library
# Rofi emoji
super + m
      rofi -modi emoji -show emoji
# Rofi
# Run Launcher
super + x
       rofi -show run
# Window swticher
super + comma
       rofi -show window
# Searchweb
super + slash
      qbrofi
# Pass
super + p
        dmenu_pass
# Screenshots
Print
    xfce4-screenshooter
# Screenlock
super +z
      xflock4
# Emacs (super + e followed by another key)
super + e
    emacsclient -c -a 'emacs'
super + c; s
    emacsclient -c -a 'emacsclient --eval "(*scratch*)"'
super + c; i
    emacsclient -c -a 'emacs' --eval '(ibuffer)'
super + o; minus
    emacsclient -c -a 'emacs' --eval '(dired nil)'
super + c; l
    emacsclient -c -a 'emacs' --eval '(elfeed)'
super + c; n
    emacsclient -c -a 'emacs' --eval '(denote)'
super + o; i
    emacsclient -c -a 'emacs' --eval '(my-mu4e-inbox)'
super + c; c
    emacsclient -c -a 'emacs' --eval '(compose-mail)'
super + c; d
    emacsclient -c -a 'emacs' --eval '(find-file "~/Dropbox/vault/log.org")'
super + c; t
    emacsclient -c -a 'emacs' --eval '(find-file "~/Dropbox/vault/todo.org")'
super + c; y
       rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
# i3 windows
super +w; x
 i3-msg kill
